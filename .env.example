# 数据库连接
# 本机开发（直接运行 npm run start:dev）：用 localhost
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/team_management?schema=public"

# 如果你把 API 跑在 docker 容器里（容器内访问数据库），才用 service 名 postgres：
# DATABASE_URL="postgresql://postgres:postgres@postgres:5432/team_management?schema=public"

# JWT 密钥（请使用强随机字符串）
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# 应用端口（可选，默认 3000）
PORT=3000
